# AL3 Minimal Missing Lemma

The weakest sufficient new ingredient is:

`local_edge_classifier_complete_and_correct`

This lemma implies:
1. nonlocal support
2. exact local delta assignment
3. edge-degree delta
4. exact ΔΘ expansion

Without AL3, full combinatorial closure is not derivable from the current repository surface.
